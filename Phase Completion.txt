NIGHTFORGE — PHASE 1 COMPLETION RECORD
======================================

Completed: 9 August 2026 (Australia/Sydney)
Project: C:\Users\Marcelo\Documents\Roll30 UI Rebuild
Phase: Application and repository foundation
Baseline tag: nightforge-ui-baseline
Baseline commit: c1cbeca


1. PHASE 1 RESULT
-----------------

Phase 1 is complete.

Nightforge now has an independently written application-state and persistence
foundation beneath the existing prototype. No original Roll30 implementation
code, frontend code, CSS, components, layouts, or storage data was copied into
this project.

The Phase 1 code is intentionally not connected to the visible Nightforge
screens yet. Connecting Library, Heroes, Scene, and Table to real data belongs
to later phases. This separation is deliberate: Phase 1 establishes and proves
the backend boundary without changing the current UI.


2. BASELINE AND VERSION-CONTROL PROTECTION
------------------------------------------

- Initialized a new Git repository inside the Nightforge folder.
- Added a project-appropriate .gitignore.
- Committed the untouched Nightforge prototype.
- Tagged the untouched visual baseline as nightforge-ui-baseline.
- Recorded SHA-256 hashes for the 12 protected Nightforge UI source files.
- Added an automated verifier that fails if any protected source file changes.
- Confirmed that no existing Nightforge screen, stylesheet, main entry point, or
  glyph component changed during Phase 1.
- Did not modify the original C:\Users\Marcelo\Documents\Roll30 repository.

Protected files:

- src/main.jsx
- src/screens/LibraryScreen.jsx
- src/screens/HeroesScreen.jsx
- src/screens/SceneScreen.jsx
- src/screens/TableScreen.jsx
- src/styles/core.css
- src/styles/shell.css
- src/styles/library.css
- src/styles/heroes.css
- src/styles/scene.css
- src/styles/table.css
- src/ui/Glyphs.jsx


3. APPLICATION FOUNDATION IMPLEMENTED
-------------------------------------

Created a new application layer with:

- A stable success result shape:
  { ok: true, value, ...metadata }
- A stable failure result shape:
  { ok: false, code, message, recovery, retryable, cause? }
- An initial application state that always starts at Library.
- A reducer for hydration, navigation, active Scene context, Scene/Hero lists,
  and explicit persistence status.
- Persistence states: idle, saving, saved, and error.
- Application commands for initialization, navigation, Scene selection, Scene
  CRUD, and Hero CRUD.
- Route validation that prevents Scene Settings or Table from opening without
  a selected Scene.
- Durable active-Scene selection with session context as a secondary convenience.
- Stale session cleanup when an active Scene is deleted.
- Preservation of active Scene context without allowing a reload to bypass
  Library.

Files:

- src/application/result.js
- src/application/state.js
- src/application/commands.js


4. CLEAN-ROOM RECORD CONTRACTS IMPLEMENTED
------------------------------------------

Implemented independently written, normalizing record factories for Scenes and
Heroes.

Scene foundation includes:

- Stable ID.
- Name with Untitled scene fallback.
- Separate play/battle Scene kind.
- Grid size clamped to the Nightforge range of 24–80 pixels.
- Independent artwork key.
- Blank-canvas state.
- Persisted artwork transform.
- Walls and wall visibility.
- Chests.
- Tokens.
- Encounter state separated from Scene kind.
- Creation, update, and last-opened timestamps.
- Schema version.
- Play Scenes cannot retain an encounter.

Hero foundation includes:

- Stable ID.
- Name with Unnamed hero fallback.
- Level clamped to 1–20.
- Fighter/Human defaults.
- Six base ability scores clamped to 8–15.
- Alignment, background, and languages.
- Save and skill proficiencies.
- Quantity inventory normalization.
- Duplicate inventory-stack merging.
- Main-hand and off-hand loadout references.
- Armor and shield references.
- Enchantment bonuses clamped to +0 through +3.
- Worn-item IDs.
- Creation and update timestamps.
- Schema version.

File:

- src/domain/records.js


5. FRESH NIGHTFORGE STORAGE IMPLEMENTED
---------------------------------------

Nightforge uses only these new identifiers:

- roll30-nightforge-v1:state
- roll30-nightforge-v1:state-backup
- roll30-nightforge-v1:session
- roll30-nightforge-assets
- scene-artwork

The verifier confirms that runtime modules do not use these original Roll30
identifiers:

- roll30-maps
- roll30-active-map
- roll30-characters
- roll30-assets

No original Roll30 save is read, imported, altered, or deleted.

File:

- src/storage/constants.js


6. VALIDATED STATE ENVELOPE IMPLEMENTED
---------------------------------------

The application envelope contains:

- Schema version.
- Monotonic revision.
- Save timestamp.
- Scenes.
- Heroes.
- Last active Scene ID.
- Pending artwork-deletion keys.
- Deterministic checksum.

Implemented behavior:

- Empty-state creation.
- Record normalization.
- Canonical serialization for deterministic checksum calculation.
- Checksum verification.
- Invalid JSON detection.
- Invalid envelope-shape detection.
- Incompatible-version detection.
- Invalid-record detection.
- Invalid active Scene references normalize to null.
- Duplicate pending artwork keys normalize safely.

File:

- src/storage/envelope.js


7. PRIMARY/BACKUP STATE REPOSITORY IMPLEMENTED
----------------------------------------------

Implemented behavior:

- Reads primary and backup state independently.
- Chooses the newest valid revision.
- Starts empty without writing when no save exists.
- Recovers from a corrupt primary using the valid backup.
- Preserves the previous valid primary before a new save.
- Increments revision on every successful save.
- Reads the just-written state back and validates it before reporting success.
- Attempts to restore the prior primary if verification fails.
- Converts read, write, and verification failures into actionable result objects.
- Keeps failed edits available to the future application layer rather than
  falsely reporting them as saved.

File:

- src/storage/stateRepository.js


8. SCENE AND HERO REPOSITORIES IMPLEMENTED
------------------------------------------

Both repositories support:

- list
- get by stable ID
- create
- update exactly one record
- remove exactly one record
- explicit not-found results
- revision-bearing save results

The Scene repository additionally supports:

- Durable active Scene selection.
- Clearing the durable active Scene reference when that Scene is removed.

File:

- src/storage/entityRepositories.js


9. SESSION REPOSITORY IMPLEMENTED
---------------------------------

Implemented behavior:

- Loads active Scene context.
- Saves active Scene context.
- Clears active Scene context.
- Uses only the isolated Nightforge session key.
- Returns explicit invalid/read/write failures.
- Never controls the startup route; Library remains the startup screen.

File:

- src/storage/sessionRepository.js


10. ARTWORK REPOSITORY FOUNDATION IMPLEMENTED
---------------------------------------------

Implemented:

- IndexedDB database opening and version-1 store creation.
- Artwork get, put, remove, and key enumeration.
- Transaction handlers attached before request completion, preventing a timing
  race in fast IndexedDB transactions.
- Explicit errors for unavailable, blocked, failed, or aborted IndexedDB work.
- A repository wrapper that converts adapter failures into actionable results.
- Dependency injection so storage can be tested without a browser.

The staged artwork replacement/deletion coordinator is intentionally scheduled
for the Scene-workbench phase. Phase 1 supplies and tests the reliable storage
boundary it will use.

Files:

- src/storage/artworkRepository.js
- src/storage/memoryAdapters.js


11. TESTING AND VERIFICATION ADDED
---------------------------------

Added package commands:

- npm test
- npm run verify:phase1
- npm run verify

Automated test coverage currently includes 23 passing tests:

- Stable command result contracts.
- Scene defaults and Scene-kind/encounter separation.
- Hero defaults.
- Hero normalization, inventory merging, and value clamping.
- Envelope sealing and tamper detection.
- Empty-storage behavior.
- State save verification and revision increments.
- Backup recovery after primary corruption.
- Write failure with prior-state preservation.
- Full Scene repository CRUD.
- Targeted Hero updates.
- Active Scene cleanup on deletion.
- Durable active Scene selection.
- Isolated session storage.
- Artwork repository round trip.
- Actual IndexedDB request/transaction lifecycle through a deterministic fake.
- Artwork failure translation.
- Library-first application hydration.
- Application initialization.
- Durable Scene selection when session storage fails.
- Stale-session cleanup after Scene removal.
- Route guards.
- Visible persistence-failure dispatch.

The Phase 1 verifier checks:

- Exact hashes for all 12 protected Nightforge UI files.
- Presence of every required Phase 1 runtime module.
- Absence of original-project paths and imports.
- Absence of original Roll30 storage-key usage.
- Presence of fresh Nightforge storage identifiers.
- Presence of test and verification commands.

Files:

- src/phase1.test.js
- scripts/nightforge-baseline-hashes.json
- scripts/verify-phase1.mjs
- package.json


12. VERIFICATION RESULTS
------------------------

Automated tests:

- Tests: 23
- Passed: 23
- Failed: 0
- Skipped: 0

Static Phase 1 verification:

- Protected UI files checked: 12
- Protected UI hash mismatches: 0
- Original-project runtime imports found: 0
- Original Roll30 runtime storage keys found: 0
- Missing Phase 1 modules: 0

Production build:

- Result: Pass
- CSS asset: index-B2GrDRvv.css
- JavaScript asset: index-Cmltj9nq.js

Those asset filenames match the untouched Nightforge build produced before
Phase 1 because the new foundation is not yet connected to the UI bundle.

Git checks:

- git diff --check: Pass
- Existing protected UI files changed: 0

Visual verification performed by the implementation agent:

- None.
- No browser tool was used.
- No screenshot tool was used.
- No automated visual comparison was performed.
- UI preservation was verified non-visually through exact source hashes,
  protected-file diffs, and unchanged production asset identities.


13. IMPORTANT PHASE BOUNDARY
----------------------------

Phase 1 creates and proves the foundation. It does not yet replace prototype
data or no-op controls in the visible Nightforge screens.

You should therefore expect:

- Nightforge to look exactly as it did before Phase 1.
- Existing dummy characters, Scenes, and tokens to remain visible.
- Existing prototype buttons to remain placeholders.
- No new browser storage keys to appear merely by opening the current UI.

Those are not Phase 1 failures. Connecting the UI to the new foundation begins
with the later Library/routing and Scene/Hero phases.


14. TESTING LIST FOR THE OWNER
------------------------------

Run these commands from:

C:\Users\Marcelo\Documents\Roll30 UI Rebuild

[ ] 1. Run the complete Phase 1 test suite:

        npm test

    Expected:
    - 23 tests.
    - 23 pass.
    - 0 fail.

[ ] 2. Run the Phase 1 structural and clean-room verifier:

        npm run verify:phase1

    Expected messages:
    - Nightforge visual baseline preserved across 12 protected files.
    - Phase 1 runtime boundary verified.
    - Fresh Nightforge storage identifiers are present.

[ ] 3. Run the production build:

        npm run build

    Expected:
    - Vite build succeeds.
    - No missing import or compilation error.

[ ] 4. Run the combined non-visual verification command:

        npm run verify

    Expected:
    - Tests pass.
    - Phase 1 verifier passes.
    - Production build passes.

[ ] 5. Confirm the protected baseline tag exists:

        git tag --list

    Expected:
    - nightforge-ui-baseline

[ ] 6. Confirm the baseline commit exists:

        git show --stat --oneline nightforge-ui-baseline

    Expected first line:
    - c1cbeca Establish untouched Nightforge visual baseline

[ ] 7. Confirm no existing Nightforge UI file is different from the baseline:

        git diff --name-only nightforge-ui-baseline -- src/main.jsx src/screens src/styles src/ui

    Expected:
    - No output.

[ ] 8. Optional manual visual sanity check performed by you:

    - Start Nightforge with npm run dev.
    - Open the provided local URL.
    - Visit Library, Heroes, Scene, Table Setup, and Table Battle.
    - Confirm the prototype looks and behaves exactly as it did before Phase 1.
    - Confirm no new panel, button, spacing change, color change, or layout shift
      appears.

    This is intentionally your manual check. The implementation agent did not
    use browser or screenshot tools.

[ ] 9. Optional browser-storage safety check performed by you:

    - Open browser developer tools while the unchanged prototype is running.
    - Inspect Local Storage.
    - Confirm simply opening the current UI has not created original Roll30 keys.
    - It is also normal for the new Nightforge keys not to exist yet because the
      Phase 1 application layer is not connected to the visible prototype.

[ ] 10. Confirm original Roll30 remains separate:

    - Open C:\Users\Marcelo\Documents\Roll30.
    - Confirm Phase 1 files were created only in Roll30 UI Rebuild.
    - Do not delete or alter any original browser saves.


15. PHASE 1 COMPLETION STATEMENT
--------------------------------

Every implementation requirement in Phase 1 has direct evidence:

- Repository foundation: implemented and tested.
- Application reducer and commands: implemented and tested.
- Scene/Hero repositories: implemented and tested.
- Session repository: implemented and tested.
- Artwork repository: implemented and tested without a browser.
- Fresh isolated storage: implemented and statically verified.
- Primary/backup recovery: implemented and tested.
- Explicit save/error status: implemented and tested.
- Original Roll30 separation: statically verified.
- Nightforge UI preservation: proven by exact protected-file hashes and unchanged
  build asset identities, without browser or screenshot tooling.

Phase 1 is complete and ready for the next implementation phase.


===============================================================================
NIGHTFORGE — PHASE 2 COMPLETION AND PATCH NOTES
===============================================================================

Completed: 12 August 2026 (Australia/Sydney)
Project: C:\Users\Marcelo\Documents\Roll30 UI Rebuild
Phase: Library and routing
Previous completed phase: Phase 1 — application and repository foundation


16. PHASE 2 RESULT
------------------

Phase 2 is complete.

Nightforge now starts from its real, fresh Nightforge-only application state.
The Library no longer contains prototype Scene records. Forge, Open, Settings,
Delete, command-deck navigation, active Scene context, and reload restoration are
connected to the independently written Phase 1 repositories.

No original Roll30 UI, UX, CSS, JSX, component, storage record, runtime module,
or browser save was imported or accessed.


17. APPLICATION BOOTSTRAP AND ROUTING
-------------------------------------

Implemented:

- Added a browser runtime that creates Nightforge-only state, Scene, Hero,
  session, and artwork repositories.
- Connected the React application to the Phase 1 reducer and commands.
- Hydrates Scenes, Heroes, durable active Scene context, and session context on
  startup.
- Every fresh page load opens Library, even when an active Scene is remembered.
- Brand and Library navigation return to Library.
- Heroes remains globally available.
- Scene navigation requires an active Scene.
- Enter the Table requires an active Scene.
- Scene and Table controls are visibly and semantically disabled when no Scene
  exists, with an explanation in their title.
- The Table remains full-screen without the command deck.
- The active Scene is supplied to Scene Settings and Table.
- Play Scenes open in Play state.
- Battle Scenes without an encounter open in Setup.
- A restored active/complete encounter is routed to Battle state.

Files:

- src/App.jsx
- src/main.jsx
- src/application/browserRuntime.js
- src/application/library.js


18. REAL LIBRARY DATA
---------------------

Implemented:

- Removed all three hardcoded prototype Scenes from Library.
- Library renders only records returned by the Nightforge Scene repository.
- Featured stage displays the most recently opened Scene.
- Remaining Scenes render in the existing Nightforge ledger.
- Scene count is derived from repository records.
- Scene identity is always stable-ID based.
- Play and Battle labels are derived from Scene kind.
- Battle Scenes show the 5-foot-grid treatment.
- Play Scenes show Free play without a false grid label.
- Scene color accents are deterministic without adding persisted presentation
  data to the Scene record.
- Stored IndexedDB artwork is loaded into the existing Nightforge art treatment.
- Missing or failed artwork falls back safely to Nightforge's existing painted
  wash.
- No production dummy Scene is seeded.

File:

- src/screens/LibraryScreen.jsx


19. FORGE WORKFLOW
------------------

The existing Forge drawer now:

- Accepts a Scene name.
- Creates either a Play or Battle Scene.
- Normalizes an empty name to Untitled scene.
- Persists the Scene before changing the visible collection.
- Makes the new Scene active in the same state-envelope save.
- Records its initial last-opened timestamp.
- Remembers active context in the isolated Nightforge session key.
- Closes only after successful persistence.
- Enters the new Scene's Table after success.
- Preserves the entered name, selected kind, and open drawer after failure.
- Shows the error and recovery instruction inside the Forge drawer.
- Exposes an honest Forging state while persistence is active.


20. OPEN AND SETTINGS WORKFLOWS
-------------------------------

Implemented:

- Featured-stage Open targets the exact Scene ID.
- Ledger thumbnail and Open button target the exact Scene ID.
- Featured and ledger Settings controls target the exact Scene ID.
- Opening a Scene updates lastOpenedAt and durable active context in one verified
  state save.
- The opened Scene becomes the featured Scene on return to Library.
- Table receives the selected Scene's real name and kind.
- Scene workbench initializes its visible name, kind, and grid size from the
  selected Scene.
- Play Table presents an honest Play state and does not display Setup/Battle or
  a 5-foot-grid label.


21. DELETE WORKFLOW
-------------------

Implemented:

- Every Delete glyph opens a Nightforge-native confirmation drawer.
- Confirmation names the exact Scene being deleted.
- The Scene remains visible until the state-envelope write is verified.
- Failed deletion keeps the Scene and active selection intact.
- Failed deletion remains open and displays an actionable error inside the
  drawer.
- Deleting a non-active Scene preserves the current active Scene.
- Deleting the active Scene chooses the safest remaining Scene by recency.
- Deleting the final Scene clears durable and session active context.
- Scene artwork keys are added to pendingArtworkDeletes only after successful
  Scene removal, ready for the staged cleanup coordinator in Phase 3.
- Original Roll30 browser data is never inspected or changed.


22. EMPTY, LOADING, ERROR, AND DISABLED STATES
----------------------------------------------

Implemented:

- Empty Library retains the Nightforge masthead, stage, ledger, and Forge
  language while teaching the first action.
- Startup loading retains the stage structure and explains that Nightforge is
  restoring this browser's campaign state.
- Repository and artwork errors identify what failed and what to do next.
- Backup recovery is reported visibly when Nightforge restores a valid backup
  after detecting an invalid primary save.
- Forge and Delete failures display in the active drawer rather than behind it.
- Busy controls are disabled and visually communicate that work is in progress.
- Escape closes the active Forge or Delete drawer without performing an action.
- Destructive action remains separated behind exact-target confirmation.


23. VISUAL AND CLEAN-ROOM PROTECTION
------------------------------------

The following six original Nightforge stylesheets remain byte-identical to the
nightforge-ui-baseline tag:

- src/styles/core.css
- src/styles/shell.css
- src/styles/library.css
- src/styles/heroes.css
- src/styles/scene.css
- src/styles/table.css

All new conditional styling lives in:

- src/styles/functional-states.css

Every selector in that file begins under an nf-state functional namespace.
It does not restyle an existing Nightforge base class by itself and does not
introduce a second visual language.

The existing Nightforge stage, ledger, masthead, command deck, drawer, buttons,
tags, typography, spacing, palette, and Table HUD remain the presentation
authority. No Roll30 presentation material was transferred.


24. PHASE 2 TESTING AND VERIFICATION
------------------------------------

New automated workflow coverage includes:

- Featured-Scene recency ordering.
- Stable presentation helpers and Play/Setup/Battle routing.
- Complete Forge persistence, selection, session, and route transition.
- Blank-name normalization.
- Exact-ID Open and last-opened refresh.
- Reload restoration with mandatory Library-first startup.
- Active-Scene deletion with safe fallback.
- Final-Scene deletion with context clearing.
- Artwork-cleanup scheduling.
- Forge write-failure preservation.
- Delete write-failure preservation.

Total automated test results:

- Tests: 34
- Passed: 34
- Failed: 0
- Skipped: 0

Render smoke coverage:

- Populated Library.
- Empty Library.
- Guarded command deck without a Scene.
- Enabled command deck with an active Scene.
- Active Scene workbench context.
- Play Table context.
- Result: Pass.

Static Phase 2 verifier:

- Six frozen stylesheet hashes match: Pass.
- Functional-state selector namespace: Pass.
- Library placeholder Scene detection: Pass; none found.
- Required Forge/Open/Settings/Delete connections: Pass.
- Active-context and browser-runtime boundaries: Pass.
- Original-project runtime paths/imports: 0.
- Original Roll30 runtime storage-key uses: 0.

Production build:

- Result: Pass.
- CSS asset: index-Dxd5srfU.css
- JavaScript asset: index-UNEsmpwi.js

Combined verification command:

- npm run verify
- Result: Pass.

Interactive browser-plugin verification:

- Not performed because the in-app browser control surface was not exposed in
  this execution session.
- No substitute browser automation was used.
- Runtime rendering was instead verified through the deterministic Vite SSR
  render smoke plus application workflow tests and production compilation.


25. IMPORTANT PHASE 2 BOUNDARY
------------------------------

Phase 2 makes Library and routing real. Later phase boundaries remain intact:

- Scene workbench inputs initialize from the active Scene, but persistent name,
  kind, artwork, blank-canvas, grid, preview, and return-context editing belongs
  to Phase 3.
- Heroes still shows prototype Hero data until Phase 4.
- Gear remains prototype data until Phase 5.
- Table camera, map tools, walls, ruler, and real Play tokens belong to Phase 6.
- Table tokens, Inspector editing, and chests remain prototype behavior until
  Phase 7.
- Initiative, movement, combat, conditions, thrown items, and ammunition remain
  scheduled for later phases.

These are explicit phase boundaries, not Phase 2 failures.


26. PHASE 2 TESTING LIST FOR THE OWNER
--------------------------------------

Run commands from:

C:\Users\Marcelo\Documents\Roll30 UI Rebuild

[ ] 1. Run all automated tests:

        npm test

    Expected:
    - 34 tests.
    - 34 pass.
    - 0 fail.

[ ] 2. Run the render smoke:

        npm run test:phase2:render

    Expected:
    - Phase 2 render smoke passed for populated, empty, guarded, settings, and
      Play Table states.

[ ] 3. Run Phase 2 structural verification:

        npm run verify:phase2

    Expected:
    - Six frozen Nightforge stylesheets match.
    - Library/routing/active-context boundaries are present.
    - Functional-state CSS is scoped.

[ ] 4. Run the complete verification command:

        npm run verify

    Expected:
    - Tests, render smoke, Phase 1 verifier, Phase 2 verifier, and build pass.

[ ] 5. Start Nightforge:

        npm run dev

    Open the local URL Vite prints.

[ ] 6. Confirm fresh startup:

    - Library appears first.
    - No Goblin Ambush, Tavern of the Salty Dog, or Dragon's Lair dummy Scene
      appears in a fresh Nightforge browser profile.
    - Scene and Enter the Table are disabled until a Scene exists.

[ ] 7. Test Forge:

    - Forge a Battle Scene named The Sunken Crypt.
    - Confirm its Table opens in Setup and shows The Sunken Crypt.
    - Return to Library.
    - Forge a Play Scene named Moonlit Parley.
    - Confirm its Table shows Free play and no Setup/Battle switch.

[ ] 8. Test Library identity:

    - Return to Library.
    - Confirm the most recently opened Scene occupies the featured stage.
    - Confirm the other Scene appears in the ledger.
    - Confirm the Scene count is 2.
    - Open each Scene and verify the exact selected name reaches Table.

[ ] 9. Test Settings identity:

    - Use each Scene's Settings glyph.
    - Confirm the workbench opens with that Scene's exact name, kind, and grid.
    - Do not use workbench editing as a Phase 2 test; persistence for those fields
      is Phase 3.

[ ] 10. Test reload behavior:

    - Open a Scene's Table.
    - Reload the page.
    - Confirm startup returns to Library.
    - Confirm Enter the Table remains enabled for the remembered active Scene.

[ ] 11. Test deletion:

    - Open Delete for one Scene and confirm its exact name appears.
    - Choose Keep scene and confirm nothing is removed.
    - Delete the active Scene and confirm the remaining Scene becomes active.
    - Delete the final Scene and confirm the real empty state appears and the
      Scene/Table command-deck actions disable again.

[ ] 12. Confirm data separation:

    - Inspect Local Storage and confirm Nightforge uses only
      roll30-nightforge-v1:* keys.
    - Confirm original roll30-maps, roll30-active-map, roll30-characters, and
      roll30-assets data remains untouched.


27. PHASE 2 COMPLETION STATEMENT
--------------------------------

Every Phase 2 plan requirement now has direct evidence:

- Placeholder replacement: implemented and render-verified.
- Forge: implemented and workflow-tested.
- Open: implemented and stable-ID tested.
- Settings: implemented with exact active context and render-verified.
- Delete: implemented with persistence-first behavior and failure tests.
- Active context: implemented across envelope, session, reducer, and route.
- Empty/loading/error/disabled states: implemented and structurally verified.
- Scene lifecycle across reload: tested.
- Mandatory Library-first startup: tested.
- Frozen Nightforge visual foundation: hash-verified.
- Clean-room and fresh-storage boundaries: statically verified.
- Production build: passed.

Phase 2 is complete and ready for Phase 3.


NIGHTFORGE - PHASE 3 COMPLETION AND PATCH NOTES
================================================

Date completed: 12 August 2026
Phase: 3 - Scene workbench
Previous completed phase: Phase 2 - Library and routing


28. PHASE 3 RESULT
------------------

Phase 3 is complete.

The existing Nightforge Scene workbench is now connected to fresh Nightforge
state and artwork repositories. The visible workbench composition, controls,
typography, spacing, and permanent styles remain the Nightforge design. No
Roll30 UI, UX, layout, stylesheet, component, or browser-save data was imported.


29. FUNCTIONALITY CONNECTED
---------------------------

- Scene name edits update the live preview and persist to the active Scene.
- Name edits use a 450 ms debounce and flush on blur or navigation.
- Save failures retain the local draft and expose an actionable Not saved state.
- Play and Battle controls persist the Scene kind.
- Battle-to-Play changes require confirmation when an encounter exists.
- Confirmed Battle-to-Play changes clear the encounter and its physical battle
  items while preserving tokens, positions, inventory, artwork, walls, chests,
  and the Scene record.
- Play-to-Battle changes preserve the Scene and open the Battle workbench state.
- Battle grid-size changes update the live preview and persist to the Scene.
- The grid control remains absent from Play Scenes.
- The preview uses the active Scene name, kind, grid size, blank-canvas state,
  and stored artwork blob.
- Upload Artwork validates and decodes an actual image before persistence.
- Use White Canvas clears the artwork reference through the recovery workflow.
- The workbench Back control returns to Library when entered from Library.
- The workbench Back control returns to the exact Table mode when entered from
  the Table Scene Settings glyph.
- The command-deck Scene action opens with Library return context.
- The existing Table Scene Settings glyph continues to open the active Scene
  directly without changing its visible placement or styling.


30. ARTWORK TRANSACTION AND RECOVERY ORDER
------------------------------------------

Artwork replacement now follows this verified sequence:

1. Confirm that the file is a non-empty image.
2. Decode the image and verify usable dimensions.
3. Generate a fresh Nightforge artwork key.
4. Store the blob under the staged key in IndexedDB.
5. Read the staged blob back and verify that it exists.
6. Save the Scene envelope with the new artwork reference.
7. Add the previous key to the durable pending-deletion set as part of that
   same Scene save.
8. Delete the prior artwork blob only after the new Scene reference is saved.
9. Remove the pending key only after blob deletion is confirmed.

Failure behavior:

- Decode failure leaves the prior Scene and artwork untouched.
- Staged-write failure leaves the prior Scene and artwork untouched.
- Staged-read failure deletes the staged orphan and keeps prior valid data.
- Missing staged readback is treated as verification failure.
- Scene-envelope save failure deletes the staged orphan and retains the prior
  Scene reference.
- Old-artwork deletion failure keeps the new valid Scene reference and a
  durable pending cleanup key.
- Cleanup-acknowledgement failure leaves the pending key retryable even when
  the blob has already been removed.
- Startup retries every pending deletion.
- Startup also reconciles and removes unreferenced staged orphan blobs.
- A failed cleanup never restores an old Scene reference or resurrects a
  deleted Scene.

White Canvas uses the same persistence-first safety model:

1. Save artworkKey as null and blankCanvas as true.
2. Durably schedule the previous key for deletion.
3. Delete the blob.
4. Acknowledge cleanup only after confirmed deletion.


31. FILES ADDED
---------------

- src/application/artwork.js
  Browser image validation and decoding boundary.

- src/phase3.test.js
  Phase 3 repository, command, transition, artwork, and recovery tests.

- scripts/phase3-render-smoke.mjs
  Render verification for Battle, Play, blank canvas, errors, grid preview,
  and Table/Library return states.

- scripts/verify-phase3.mjs
  Static Phase 3 integration, purity, and frozen-visual verifier.


32. FILES UPDATED
-----------------

- src/App.jsx
  Connects the Scene workbench to application commands and preserves entry
  context. Flushes final draft values before leaving the workbench.

- src/screens/SceneScreen.jsx
  Connects the existing controls to real Scene and artwork behavior without
  replacing the Nightforge workbench composition.

- src/screens/TableScreen.jsx
  Carries the current Table mode into Scene Settings return context.

- src/application/commands.js
  Adds staged artwork replacement, White Canvas, cleanup retry, orphan
  reconciliation, and Scene-workbench persistence orchestration.

- src/application/browserRuntime.js
  Supplies the browser artwork decoder and artwork repository to commands.

- src/storage/entityRepositories.js
  Adds atomic Scene artwork-reference updates and durable cleanup scheduling
  and acknowledgement operations.

- package.json
  Adds Phase 3 render and structural verification to the full gate.


33. VERIFICATION EVIDENCE
-------------------------

Full command:

    npm run verify

Result: Pass.

- Node test suite: 49 passed, 0 failed.
- Phase 2 render smoke: Pass.
- Phase 3 render smoke: Pass.
- Phase 1 verifier: Pass.
- Phase 2 verifier: Pass.
- Phase 3 verifier: Pass.
- Production Vite build: Pass.
- Permanent Nightforge visual hashes: unchanged.
- Runtime references to the original Roll30 project: none.
- Original Roll30 storage access from Nightforge runtime: none.
- Nightforge live development server: HTTP 200 at the time of completion.

Phase 3 test coverage includes:

- Successful artwork stage, verification, commit, and cleanup.
- Invalid/decode-failed artwork.
- Staged write failure.
- Staged read failure.
- Missing staged readback.
- Scene-envelope save failure after staging.
- Previous-artwork deletion failure.
- Pending cleanup retry at startup.
- Unreferenced orphan reconciliation.
- Cleanup acknowledgement failure and later retry.
- Successful and failed White Canvas operations.
- Battle-to-Play destructive-state clearing and asset preservation.
- Play-to-Battle, name, and grid persistence.
- Browser image-type and decoded-dimension validation.


34. UI AND DATA BOUNDARY AUDIT
------------------------------

- No permanent Nightforge stylesheet changed.
- The Nightforge glyph component hash is unchanged.
- No Roll30 source file was read into or imported by Nightforge runtime code.
- No Roll30 UI, UX, layout, CSS, JSX, or component family was copied.
- No Roll30 characters, games, Scenes, artwork, battles, or browser saves were
  imported.
- Nightforge continues using only its isolated LocalStorage, SessionStorage,
  and IndexedDB identifiers.
- New visual states are conditional functional states within the existing
  Nightforge workbench rather than a redesign.


35. PHASE 3 COMPLETION STATEMENT
--------------------------------

Every Phase 3 plan requirement has direct implementation and verification
evidence:

- Name: connected, debounced, flushed, persisted, and failure-safe.
- Type: connected with correct Play/Battle transitions and confirmation.
- Artwork: decoded, staged, verified, committed, displayed, and recoverable.
- Blank canvas: persisted with cleanup-safe removal ordering.
- Grid: Battle-only, previewed at the actual size, and persisted.
- Preview: connected to the active Scene and real stored artwork.
- Return context: Library and exact Table-mode paths verified.
- Artwork replacement and cleanup: every defined failure branch preserves the
  last valid Scene data and remains safely retryable.
- Play/Battle behavior: encounter clearing and Scene asset preservation tested.
- Nightforge interface preservation: frozen permanent visual hashes verified.
- Clean-room and fresh-data constraints: statically verified.
- Production build: passed.

Phase 3 is complete and ready for Phase 4.


NIGHTFORGE - PHASE 4 COMPLETION AND PATCH NOTES
================================================

Date completed: 12 August 2026
Phase: 4 - Hero domain and UI
Previous completed phase: Phase 3 - Scene workbench


36. PHASE 4 RESULT
------------------

Phase 4 is complete.

The existing Nightforge Heroes portrait rail, codex header, vitals ribbon,
chapter tabs, Identity sheet, and six ability dials now use independently
implemented Hero rules and fresh Nightforge persistence. No original Roll30
React, CSS, layout, storage, characters, or implementation code was copied.

The protected Nightforge Heroes stylesheet and every other permanent visual
file remain byte-identical to the approved baseline. New conditional and
below-the-fold functional states use only the scoped nf-state namespace and
existing Nightforge variables and primitives.


37. HERO LIFECYCLE
------------------

- The portrait rail renders persisted Heroes rather than fixture characters.
- Portrait selection targets stable Hero IDs.
- Both existing New hero actions create a real level-1 Human Fighter, persist
  it, and select it.
- Fresh Heroes begin with all six purchased abilities at 8 and 27 points left.
- Hero name and background drafts debounce for 450 ms.
- Final drafts flush on blur, Hero selection, creation, and navigation.
- Failed saves retain the local text draft and show Not saved with recovery.
- Retire hero opens a Nightforge confirmation naming the exact Hero.
- Successful retirement removes only the Hero record.
- Failed retirement leaves the Hero visible and unchanged.
- Existing Scene token snapshots remain unchanged after their source Hero is
  edited or retired.
- A fresh empty roster uses the existing Nightforge codex language and contains
  no fake production Heroes.


38. CLASS RULES
---------------

Implemented classes:

- Fighter: d10 Hit Die, Strength and Constitution saving throws, no casting.
- Wizard: d6 Hit Die, Intelligence and Wisdom saving throws, placeholder
  spellcasting only.

Changing class is enforced at the application-command boundary as well as the
screen: saving throws reset to the selected class defaults and selected skills
reset to empty.

Wizard scaffold:

- Spell save DC = 8 + proficiency bonus + Intelligence modifier.
- Spell attack = proficiency bonus + Intelligence modifier.
- Slots display as unlimited.
- Spells display as unlimited.
- The UI explicitly states that no spell list, slot economy, or in-battle
  casting exists.


39. RACE AND IDENTITY RULES
---------------------------

All nine required races are implemented:

- Dwarf.
- Elf.
- Halfling.
- Human.
- Dragonborn.
- Gnome.
- Half-Elf.
- Half-Orc.
- Tiefling.

All four required subraces are implemented:

- Hill Dwarf.
- High Elf.
- Lightfoot Halfling.
- Rock Gnome.

Implemented traits are deliberately limited to the current numeric/flavor
scope: ability bonuses, speed, size, and granted languages. Darkvision,
resistances, Breath Weapon, and other future effects remain excluded.

The Identity chapter now persists:

- Name.
- Class.
- Level from 1 through 20.
- Race.
- Conditional subrace.
- Read-only derived size.
- All nine alignments.
- Free-text background with 13 SRD suggestions.
- All 16 SRD languages as toggle chips.

Granted languages cannot be removed while their race grants them. Changing
race or subrace replaces old grants, installs new grants, and retains languages
the user selected independently.


40. POINT BUY AND DERIVED VALUES
--------------------------------

- Six independently purchased base abilities.
- Minimum purchased score 8.
- Maximum purchased score 15.
- Standard cost curve: 0, 1, 2, 3, 4, 5, 7, and 9 points for scores 8-15.
- Total budget 27.
- Overspending is blocked in the UI and normalized at the record boundary.
- Racial and subracial bonuses apply only after purchased scores.
- Each dial shows base score, final score, modifier, and legal step controls.
- Remaining budget updates from real point-buy state.

Derived values:

- Proficiency bonus from level 1 through 20.
- HP using full class Hit Die at level 1.
- Additional Fighter HP using 6 + Constitution modifier per level.
- Additional Wizard HP using 4 + Constitution modifier per level.
- Initiative from Dexterity modifier.
- Base speed and size from race.
- Ten-foot heavy-armour speed penalty when Strength is below the requirement.
- Unarmoured, light, medium-capped, and heavy Armour Class calculations.
- Shield and persisted armour/shield enchantment contributions.
- Wizard spell save and attack values.


41. SKILLS AND SAVING THROWS
----------------------------

- All six saving throws render with live modifiers and proficiency toggles.
- All 18 SRD skills render with their exact governing ability.
- A selected proficiency adds the level-based proficiency bonus.
- An unselected proficiency uses only the governing ability modifier.
- Fighter begins with Strength and Constitution saves selected.
- Fighter guidance displays the live selected count out of two.
- Selecting more than two skills remains allowed and displays a warning.
- No in-combat skill or saving-throw roller was added.


42. PHASE BOUNDARY
------------------

Gear catalogs, inventory operations, item drawers, equipment legality, and
magic-item controls remain assigned to Phase 5. The existing Gear chapter is
honestly disabled with an explanation rather than showing prototype inventory
or fake enabled controls. No Phase 5 catalog data was introduced in Phase 4.


43. FILES ADDED
---------------

- src/domain/heroes.js
  Independently authored classes, races, subraces, identity catalogs, point-buy
  rules, derivations, saves, skills, and Wizard scaffold.

- src/phase4.test.js
  Phase 4 catalog, derivation, transition, lifecycle, failure, and isolation
  tests.

- scripts/phase4-render-smoke.mjs
  Render verification for fresh-empty, Identity, Fighter Abilities, Fighter
  over-selection, Wizard, retirement, disabled Gear, and error states.

- scripts/verify-phase4.mjs
  Structural counts, integration checks, clean-room checks, scoped-functional-
  CSS checks, and permanent visual hash verification.


44. FILES UPDATED
-----------------

- src/screens/HeroesScreen.jsx
  Replaces prototype fixtures and inert Hero controls with persisted Hero data
  and Phase 4 behavior while retaining Nightforge composition and landmarks.

- src/domain/records.js
  Enforces valid class, race, subrace, alignment, language, point-buy,
  proficiency, and default-save data.

- src/application/commands.js
  Enforces class and race transition policies outside the UI.

- src/App.jsx
  Supplies repository Heroes and lifecycle commands and flushes drafts before
  leaving Heroes.

- src/styles/functional-states.css
  Adds only nf-state-scoped empty, skills, saves, and Wizard continuation
  layouts using existing Nightforge design tokens.

- package.json
  Adds Phase 4 render and structural verification to the complete gate.


45. VERIFICATION EVIDENCE
-------------------------

Full command:

    npm run verify

Result: Pass.

- Node test suite: 72 passed, 0 failed.
- Phase 2 render smoke: Pass.
- Phase 3 render smoke: Pass.
- Phase 4 render smoke: Pass.
- Phase 1 verifier: Pass.
- Phase 2 verifier: Pass.
- Phase 3 verifier: Pass.
- Phase 4 verifier: Pass.
- Production Vite build: Pass.
- Permanent Nightforge visual hashes: unchanged.
- Protected heroes.css hash: unchanged.
- Functional-state CSS selectors: fully nf-state scoped.
- Runtime references to original Roll30 files: none.
- Original Roll30 browser-save identifiers used by runtime: none.

Focused tests prove:

- Exact 2-class profiles.
- Exact 9-race and 4-subrace numeric trait matrix.
- Nine alignments and 16 languages.
- Six saves and exact mappings for all 18 skills.
- Every point-buy cost and legal boundary.
- Exact-budget acceptance and overspend prevention.
- Racial bonuses after purchased scores.
- Proficiency progression at every level boundary.
- Fighter and Wizard HP at normal, level-1, and level-20 boundaries.
- Initiative, speed, size, every armour category, shield, enchantment, and heavy
  speed penalty.
- Wizard DC, attack, and placeholder-only state.
- Proficient and non-proficient save/skill modifiers.
- Valid defaults and corrupt-input normalization.
- Exact stable-ID Hero CRUD.
- Class and race transition policies.
- Hero retirement preserving Scene token snapshots.
- Create and retire storage-failure preservation.
- Fresh empty, populated Identity, Fighter, Wizard, over-guidance, retirement,
  disabled Gear, and Not saved render states.


46. UI AND DATA BOUNDARY AUDIT
------------------------------

- No Roll30 UI, UX, layout, CSS, React, component, or test was copied.
- No Roll30 characters, Scenes, games, artwork, battles, or browser saves were
  imported, shared, migrated, deleted, or modified.
- The original app was used only as the plan-authorized read-only behavioral
  oracle to verify observable race trait outputs.
- No original Roll30 module is imported by Nightforge runtime or tests.
- Existing Nightforge top composition and protected stylesheet remain intact.
- Identity continues inside the existing Identity sheet.
- Abilities continues beneath the existing six dials in the same chapter flow.
- No original-style character form or panel was introduced.
- No future class features, feats, spellcasting, tool proficiency, or combat
  check/save mechanics were added.


47. PHASE 4 COMPLETION STATEMENT
--------------------------------

Every Phase 4 plan requirement has direct evidence:

- Classes: Fighter and Wizard catalogued, derived, persisted, and tested.
- Races: all nine and all four subraces trait-tested and rendered through real
  selection controls.
- Point buy: all costs, limits, budget, racial application, and UI states tested.
- Derived values: HP, AC, initiative, speed, size, proficiency, heavy penalty,
  and Wizard values tested.
- Hero CRUD: real create, stable-ID select/update/delete, reload persistence,
  failure preservation, empty state, and token independence implemented.
- Identity chapter: every specified field connected.
- Abilities chapter: six real dials, six saves, 18 skills, live modifiers,
  proficiency state, and class guidance connected.
- Wizard scaffold: correct values with no real casting surface.
- Nightforge appearance: permanent visual hashes unchanged and all additions
  conditionally scoped.
- Clean-room and fresh-data limits: verified.
- Production build: passed.

Phase 4 is complete and ready for Phase 5.


===============================================================================
NIGHTFORGE - PHASE 5 COMPLETION AND PATCH NOTES
===============================================================================

Date completed: 16 August 2026 (Australia/Sydney)
Phase: 5 - Catalogs and Gear
Previous completed phase: Phase 4 - Hero domain and UI


48. PHASE 5 RESULT
------------------

Phase 5 is complete.

Nightforge now has independently generated equipment and non-battle magic-item
catalogs, persisted Hero inventory, legal equipment loadouts, enchantments, six
implemented worn magic effects, and a functional Gear chapter. The existing
Nightforge chapter, loot-row, stepper, button, drawer, typography, spacing, and
color language remain the interface foundation.

No Roll30 UI, UX, layout, CSS, React component, catalog module, runtime code,
character, game, Scene, browser save, or other user data was copied or imported.
The original Roll30 project remained a read-only behavioral and source-corpus
reference. Its already-dirty working tree was not modified by Phase 5.


49. CLEAN-ROOM CATALOG GENERATION
---------------------------------

The catalog generator reads only the local public SRD corpus and the documented
magic-item classification index supplied with that corpus:

- 5e-SRD-Equipment.json
- 5e-SRD-Magic-Items.json
- MAGIC_ITEM_FILE_SYSTEM.txt

The generator independently:

- Excludes Net because its special restraint mechanics remain outside the
  current feature boundary.
- Separates the four ammunition entries from mundane adventuring gear.
- Derives the unique 113-item NON BATTLE set from the classification index and
  joins those names to the public SRD magic records.
- Extracts the six explicitly implemented worn accessories from the SRD magic
  records.
- Normalizes costs to copper for deterministic sorting.
- Records source names, categories, properties, damage, weight, and range data.
- Applies only the seven intentional Nightforge battle-map range overrides.
- Leaves thrown ranges source-authentic.
- Refuses generation if any required source record or exact count is missing.

Exact generated counts:

- 36 weapons.
- 4 ammunition types.
- 13 armour entries, including Shield.
- 183 mundane gear entries.
- 113 inert non-battle magic items.
- 6 implemented worn magic items.
- 355 unique item IDs in the combined catalog.

A source manifest records SHA-256 identities for all three inputs and the
generated runtime module. The Phase 5 verifier rejects any unexplained change
to the generated artifact.


50. CATALOG SEARCH, FILTERS, AND SORTING
----------------------------------------

The Add item drawer exposes backed controls for:

- Text search.
- Item type.
- Weapon class.
- Armour class.
- Gear category.
- Magic rarity.
- Weapon property.
- Damage type.
- Range band.
- Name A-Z.
- Cost low-to-high.
- Cost high-to-low.

Unpriced magic items remain after priced records in either cost direction.
The result count is live, the result area scrolls inside the Nightforge drawer,
owned quantities are shown, and an empty filtered result has an honest state.

Intentional ranged values:

- Blowgun: 25/50 feet.
- Sling: 30/60 feet.
- Hand Crossbow: 40/80 feet.
- Shortbow: 40/80 feet.
- Light Crossbow: 60/120 feet.
- Heavy Crossbow: 80/160 feet.
- Longbow: 80/160 feet.


51. INVENTORY CONTRACT AND RECOVERY
-----------------------------------

Implemented inventory behavior:

- Every stack is stored as itemId plus a positive integer quantity.
- Duplicate item IDs merge during normalization.
- Quantity zero removes the stack.
- Active operations reject unknown item IDs.
- Normalization removes unknown entries from active inventory and retains their
  IDs in recoveryDiagnostics.unknownInventoryItemIds.
- Removing an item normalizes main hand, off hand, armour, shield,
  enchantments, and worn effects.
- Ammunition uses its SRD bundle size in catalog and Gear quantity workflows.
- A separate non-catalog workflow supports individual-unit changes for the
  later combat ammunition phase.
- Inventory, loadout, enchantments, and worn state survive repository reload.

The Gear header displays both unique-stack and total-unit summaries. Its search
field filters only the Hero's owned inventory.


52. EQUIPMENT LEGALITY
----------------------

The independently written equipment domain enforces:

- Only owned weapons can occupy either hand.
- Only owned body armour can occupy the armour slot.
- Only an owned Shield can occupy the shield slot.
- Equipping the same weapon in both hands requires quantity two.
- An off-hand weapon requires an owned main-hand weapon.
- Both dual-wielded weapons must be Light melee weapons.
- A true Two-Handed weapon requires an empty off hand.
- A true Two-Handed weapon cannot be used with a shield.
- A shield requires a free off hand.
- Persisted illegal combinations normalize to a safe legal state.
- Versatile uses its larger damage die only when both the off hand and shield
  slots are empty.
- The attack-selection helper returns equipped weapons only.

Illegal actions return a stable failure and leave the persisted Hero unchanged.
The Gear continuation shows the current main hand, off hand, armour, and shield
through the existing Nightforge field language and explains rejected changes.


53. ENCHANTMENTS AND WORN MAGIC
-------------------------------

Implemented enchantments:

- Owned weapons accept +1 through +3 to attack and damage.
- Owned armour and Shield accept +1 through +3 to Armour Class.
- Bonuses remain keyed by stable item ID.
- Values clamp at +3 and are removed at +0.
- Ammunition, mundane gear, inert magic items, and unowned equipment are
  rejected.

Implemented worn effects:

- Bracers of Archery: +2 ranged weapon damage.
- Bracers of Defense: +2 AC while unarmoured and shieldless.
- Cloak of Protection: +1 AC and +1 calculated saving throws.
- Ring of Protection: +1 AC and +1 calculated saving throws.
- Ioun Stone of Mastery: +1 attacks, including the Wizard attack scaffold.
- Ioun Stone of Protection: +1 AC.

Every effect requires ownership and the Worn state. Effects stack, and no
attunement cap is imposed. Removing the item removes its active effect.


54. NIGHTFORGE GEAR INTERFACE
-----------------------------

The previously disabled Gear chapter now uses real Hero inventory while
preserving the established Nightforge composition:

- Existing loot rows render owned inventory.
- Existing steppers control quantity and enchantment.
- Existing Wear/Worn treatment controls the six implemented accessories.
- Existing Remove glyph removes the complete stack.
- Add item opens a Nightforge drawer.
- Selecting an owned loot row opens a conditional item/equipment drawer.
- Weapon, body-armour, Shield, wearable, and inert-item states receive only the
  controls applicable to that item.
- Loadout controls continue below the existing loot list.
- Drawers close from their close button, backdrop, and Escape.
- Gear drawers are portalled at document level in the browser.
- Empty inventory and empty catalog search have explicit Nightforge states.

All new conditional styling is confined to functional-states.css and every new
selector begins with the nf-state namespace. No permanent Nightforge stylesheet
or glyph source changed.


55. FILES ADDED
---------------

- scripts/generate-phase5-catalogs.mjs
  Deterministic clean-room catalog generator with exact-count gates.

- scripts/phase5-catalog-manifest.json
  Source and generated-artifact SHA-256 evidence.

- src/domain/catalog.generated.js
  Static runtime catalog generated from the documented SRD inputs.

- src/domain/catalog.js
  Combined index, facets, subtitles, cost handling, range bands, filters, and
  sorting.

- src/domain/items.js
  Inventory normalization and operations, equipment legality, loadout repair,
  enchantments, worn effects, effective damage dice, and attack selection.

- src/screens/GearChapter.jsx
  Functional Nightforge Gear chapter, catalog drawer, item drawer, and loadout
  continuation.

- src/phase5.test.js
  Catalog, inventory, equipment, magic, derivation, and reload tests.

- scripts/phase5-render-smoke.mjs
  Server-render verification for the Gear chapter and drawer states.

- scripts/verify-phase5.mjs
  Exact counts, generated-source integrity, visual hashes, scoped CSS,
  integration, clean-room, storage isolation, and text-encoding verifier.


56. FILES UPDATED
-----------------

- src/domain/records.js
  Catalog-aware inventory normalization, recovery diagnostics, and legal
  equipment normalization.

- src/domain/heroes.js
  Real equipment lookup and implemented worn AC, save, attack, and ranged
  damage contributions.

- src/screens/HeroesScreen.jsx
  Enables the existing Gear chapter and connects the functional continuation.

- src/styles/functional-states.css
  Adds only nf-state-scoped functional drawer, catalog, loadout, empty, and
  responsive states.

- src/phase4.test.js
  Supplies owned equipment to the Phase 4 derivation fixture now that Phase 5
  enforces the ownership invariant.

- scripts/phase4-render-smoke.mjs and scripts/verify-phase4.mjs
  Recognize the later-phase Gear integration without weakening Phase 4 checks.

- package.json
  Adds Phase 5 render and structural verification to the complete gate.


57. VERIFICATION EVIDENCE
-------------------------

Full command:

    npm run verify

Result: Pass.

- Node test suite: 95 passed, 0 failed.
- Phase 2 render smoke: Pass.
- Phase 3 render smoke: Pass.
- Phase 4 render smoke: Pass.
- Phase 5 render smoke: Pass.
- Phase 1 verifier: Pass.
- Phase 2 verifier: Pass.
- Phase 3 verifier: Pass.
- Phase 4 verifier: Pass.
- Phase 5 verifier: Pass.
- Production Vite build: Pass.
- Permanent Nightforge visual hashes: unchanged.
- Protected heroes.css hash: unchanged.
- Functional-state CSS selectors: fully nf-state scoped.
- Runtime references to original Roll30 files: none.
- Original Roll30 save identifiers used by runtime: none.
- Generated catalog SHA-256: matches the recorded source manifest.
- Nightforge live development server: HTTP 200 at completion.

The in-app browser control surface was unavailable in this session. Interactive
state coverage was therefore performed with the Phase 5 render suite, pure
domain event tests, repository reload tests, production compilation, and the
running-server HTTP check. No claim of screenshot or automated browser-click
comparison is made.


58. UI AND DATA BOUNDARY AUDIT
------------------------------

- No Roll30 UI, UX, layout, JSX, CSS, component, catalog module, or runtime
  implementation was copied.
- No Roll30 characters, games, Scenes, artwork, battles, browser saves, or
  inventory records were imported or migrated.
- Catalog records were regenerated from documented SRD inputs, not copied from
  the original generated catalog modules.
- The original application was not imported or referenced by Nightforge
  runtime or tests.
- Nightforge continues using only its isolated storage identifiers.
- All permanent Nightforge visual files remain byte-identical to baseline.
- The Gear implementation reuses Nightforge's own existing Gear landmarks and
  visual primitives.
- No Phase 6 Table, Phase 7 Setup, or later combat behavior was pulled forward.


59. PHASE 5 COMPLETION STATEMENT
--------------------------------

Every Phase 5 plan requirement has direct implementation and verification
evidence:

- Exact catalogs: generated, counted, hashed, and tested.
- Filters and sorting: every specified field and direction tested.
- Ranges: all seven intentional overrides and source-authentic thrown examples
  tested.
- Inventory: stack normalization, unknown recovery, zero removal, bundle steps,
  individual-unit boundary, persistence, and removal repair tested.
- Equipment: every ownership, quantity, Light, melee, Two-Handed, shield,
  Versatile, normalization, and equipped-attack branch tested.
- Enchantments: eligible types, ownership, clamping, attack, damage, and AC
  contributions tested.
- Worn magic: all six effects, conditions, stacking, ownership, removal, saves,
  AC, attack, ranged damage, and no-cap behavior tested.
- Gear chapter: owned rows, inventory search, Add item, full catalog filters,
  quantity, enchantment, Wear/Worn, removal, item drawers, loadout continuation,
  empty states, and document-level drawer boundary implemented and rendered.
- Nightforge appearance: permanent visual hashes unchanged and new states
  conditionally scoped.
- Clean-room and fresh-data constraints: verified with no original runtime
  imports, paths, storage use, or data migration.
- Production build and complete Phase 1-5 verification gate: passed.

Phase 5 is complete and ready for Phase 6.


PHASE 4 POST-COMPLETION PATCH - TEXT ENCODING
==============================================

Date: 12 August 2026

Corrected malformed UTF-8/mojibake sequences in user-visible Hero and Scene
text without changing UI structure, layout, styling, or behavior.

Examples:

- `Level 1 Fighter Â· Human` now renders as `Level 1 Fighter · Human`.
- Negative modifiers such as `âˆ’1` now render as `−1`.
- Broken ellipses, em dashes, middle dots, and infinity symbols were corrected.

Regression protection:

- Phase 4 structural verification now rejects malformed UTF-8 patterns in all
  runtime source files.
- Phase 4 render smoke explicitly verifies the corrected Hero level separator,
  Unicode minus sign, and absence of mojibake in every covered Hero state.
- A complete runtime scan reports zero malformed text occurrences.
- `npm run verify` passes with 72 tests, all render suites, all phase verifiers,
  unchanged permanent visual hashes, and a successful production build.
